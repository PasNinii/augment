/**
 * Should be synchronized with backend interface
 */
export enum DigestType {
  // BOOK = 'book',
  EXPRESSION = 'expression',
  FACT = 'fact',
  // MOVIE = 'movie',
  // PAINT = 'paint',
  QUOTE = 'quote',
  WORD = 'word',
}
